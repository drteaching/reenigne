.venv
__pycache__
*.db
.env
.pytest_cache

# Packaging metadata for the shared prompts distribution, used only by the
# worker's local install. Excluded so it cannot influence the Vercel build,
# which installs from requirements.txt. reenigne_prompts/ itself IS uploaded
# and is imported directly — apps/api is the deployment root.
pyproject.toml
tests
Makefile
pytest.ini
requirements-dev.txt
