# Root-level convenience — prefer apps/api/.env for the server.
# Desktop / CLI never need provider keys when using the cloud API.

REENIGNE_API_URL=http://localhost:8000
REENIGNE_API_TOKEN=
REENIGNE_DEFAULT_MODEL=grok-4
REENIGNE_FRAME_INTERVAL=3.0
REENIGNE_OUTPUT_DIR=~/reenigne
